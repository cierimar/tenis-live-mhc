name: update-mixed
on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  mixed:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Generar mixed.json
        shell: pwsh
        env:
          UA_MIXED: Mozilla/5.0 (Windows NT 10.0; Win64; x64) Chrome/126
        run: ./scripts/update-mixed.ps1

      - name: Commit si hay cambios
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add mixed.json web-github/mixed.json 2>/dev/null || true
          if git diff --cached --quiet; then
            echo "Sin cambios - nada que commitear"
          else
            git commit -m "mixed.json auto-update [skip ci]"
            git pull --rebase origin main || true
            git push
          fi
